[pytest]
markers =
    no_auto_mock: Opt out of autouse external service mock
    real_api: Real-API contract tests (cost money, manual run only). KIK-733
testpaths = tests
norecursedirs = tests/integration
